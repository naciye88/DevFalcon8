BasicClac first = new BasicClac();
integer sumOfTwo = first.add(6, 3);
system.debug('Sum of the two numbers is ' + sumOfTwo);

decimal averageOfTwo = first.average(20, 5);
system.debug('Average of two numbers is ' + averageOfTwo);